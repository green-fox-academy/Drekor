#include "function.h"
/*
void xYCalculator(coordinates coords)
{
    for (int i < sizeof(coordinates.x); i++)
    {
        strcat(storeCoords,coordinates.x);
        strcat(storeCoords,coordinates.y);

        printf("%d\n",storeCoords[i]);
    }
}
*/
