'use stritc'
export { };

// Given base and n that are both 1 or more, compute recursively (no loops)
// the value of base to the n power, so powerN(3, 2) is 9 (3 squared).

function baseAndN(base: number, n: number) {
  if () {

  } esle{

  }
}
console.log(baseAndN(3, 2));